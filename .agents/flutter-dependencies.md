# Flutter Dependencies

Prefer existing project packages. For new projects or missing choices:

- State: `flutter_bloc`
- Routing: `go_router`
- HTTP/API: `dio`

Rules:

- Ask before adding or changing dependencies.
- Do not add duplicate packages for one responsibility.
- Check maintenance, platform support, and compatibility before suggesting a package.
